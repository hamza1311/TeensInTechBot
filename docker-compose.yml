version: '2'
services:
  bot:
    build: .
    ports:
      - '443:443'
    depends_on:
      - db
  db:
    image: mongo:latest
    ports:
      - '27017:27017'
    volumes:
      - 'db-data:/data/db'

volumes:
  db-data: